[build]
builder = "NIXPACKS"
buildCommand = "poetry install --no-dev && poetry run prisma generate"

[deploy]
startCommand = "poetry run prisma migrate deploy && poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

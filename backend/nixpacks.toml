[phases.setup]
nixPkgs = ["python311", "poetry"]

[phases.install]
cmds = ["poetry install --no-dev"]

[phases.build]
cmds = ["poetry run prisma generate"]

[start]
cmd = "poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
